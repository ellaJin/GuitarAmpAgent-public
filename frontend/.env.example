# Base URL of the backend API.
# Falls back to http://127.0.0.1:8000 in code if unset, so local dev works
# with no .env file at all. Set this for any deployed frontend build.
VITE_API_BASE_URL=http://127.0.0.1:8000
